[PROCESS_TABLE + ([SYSTEM_STATUS_TABLE + 1]*16) + 4] = TERMINATED;
alias pid R0;
alias entry R1;
pid = 1;
while(pid < 16) do
	entry = PROCESS_TABLE + pid*16;
	if ([entry + 4] != TERMINATED) then
		call SCHEDULER;
	endif;
	pid = pid + 1;
endwhile;
halt;

